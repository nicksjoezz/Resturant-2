import ReservationsManager from '@/components/admin/ReservationsManager';

export const metadata = { title: 'Reservations — Foddo Admin' };

export default function AdminReservationsPage() {
  return <ReservationsManager />;
}
