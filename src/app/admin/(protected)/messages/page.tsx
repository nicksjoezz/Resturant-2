import MessagesInbox from '@/components/admin/MessagesInbox';

export const metadata = { title: 'Messages — Foddo Admin' };

export default function AdminMessagesPage() {
  return <MessagesInbox />;
}
